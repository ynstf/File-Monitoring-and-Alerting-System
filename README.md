# File-Monitoring-and-Alerting-System
- Server running an application generating log files (e.g., `/var/log/myapp.log`).
- Need to monitor these log files for specific error messages like "ERROR" or "CRITICAL".
